# 拖拽排序 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为待办列表实现 Edge 风格拖拽排序（中线判定 + 单元素交换），支持桌面端 DnD 和移动端 Touch。

**Architecture:** 新建独立模块 `modules/todo/features/sortable.js`，通过 `enableSortable(listEl, options)` 接口暴露。主模块 `app.js` 在 `render()` 后调用初始化，通过 `onSortEnd` 回调更新数据。

**Tech Stack:** 原生 HTML5 Drag and Drop API + Touch Events + CSS FLIP 动画

---

### 文件结构

| 文件 | 职责 |
|------|------|
| `modules/todo/features/sortable.js`（新建） | 拖拽核心：事件监听、中线判定、DOM 交换、FLIP 动画 |
| `modules/todo/app.js`（修改） | 新增 `initSortable()` 调用 + render 中绑定事件 |
| `modules/todo/style.css`（修改） | 新增浮动克隆、drag-hidden、占位符样式 |
| `index.html`（修改） | 新增 sortable.js 脚本引用 |

---

### Task 1: 建立文件骨架

**Files:** Create `modules/todo/features/sortable.js`, Modify `index.html`

- [ ] **Step 1: 创建空模块 + 注册到 index.html**

```bash
mkdir -p modules/todo/features
```

`index.html`，在 `</body>` 前，shell.js 之前加入：

```html
<script src="modules/todo/features/sortable.js"></script>
```

- [ ] **Step 2: sortable.js 写空壳接口**

```js
/* ================================================================
   sortable.js — 可拔插拖拽排序模块
   ================================================================ */

function enableSortable(listEl, options) {
  if (!listEl) { console.error('[Sortable] 缺少列表元素'); return () => {}; }
  const opt = Object.assign({ touchDelay: 300 }, options);

  // TODO: 实现

  return () => { /* destroy */ };
}

function bindSortableItem(li, data) {
  if (!data || !li) return;
  li.draggable = true;
  li.addEventListener('dragstart', data.onDragStart);
  li.addEventListener('touchstart', data.onTouchStart, { passive: false });
  li.addEventListener('touchmove', data.onTouchMove, { passive: false });
  li.addEventListener('touchend', data.onTouchEnd);
}
```

- [ ] **Step 3: 验证页面加载不报错**

打开 `http://localhost:8000`，控制台无 404 且 `enableSortable` 全局可用。

- [ ] **Step 4: Commit**

```bash
git add modules/todo/features/sortable.js index.html
git commit -m "v1.5.0"
```

---

### Task 2: 桌面端 DnD — 中线判定 + 交换

**Files:** Modify `modules/todo/features/sortable.js`

- [ ] **Step 1: 添加状态变量和辅助函数**

在 `enableSortable` 内部：

```js
let items = [];
let dragId = null;
let dragEl = null;
let floating = null;
let active = false;
let lastSwapped = null;

function refreshItems() {
  items = Array.from(listEl.querySelectorAll('.todo-item:not(.drag-floating)'));
}

function isSameGroup(id1, id2) {
  if (!opt.isSameGroup) return true;
  return opt.isSameGroup(id1, id2);
}

function crossedMidline(clientY, el) {
  const r = el.getBoundingClientRect();
  return clientY < r.top + r.height / 2;
}
```

- [ ] **Step 2: 实现浮动克隆**

```js
function createFloating(el, x, y) {
  const clone = el.cloneNode(true);
  clone.className = 'todo-item drag-floating';
  clone.style.cssText = `
    position:fixed; left:${x}px; top:${y}px;
    width:${el.offsetWidth}px; z-index:500; pointer-events:none;
    box-shadow:0 12px 40px rgba(0,0,0,0.22); border-radius:10px;
    background:#fff; transform:scale(1.02);
  `;
  document.body.appendChild(clone);
  return clone;
}

function removeFloating() {
  if (floating?.parentNode) floating.parentNode.removeChild(floating);
  floating = null;
}
```

- [ ] **Step 3: 实现 FLIP 交换动画**

```js
function performSwap(targetId) {
  if (!isSameGroup(dragId, targetId)) return;
  const srcEl = listEl.querySelector(`.todo-item[data-id="${dragId}"]`);
  const tgtEl = listEl.querySelector(`.todo-item[data-id="${targetId}"]`);
  if (!srcEl || !tgtEl || srcEl === tgtEl) return;

  // FLIP
  const rA = srcEl.getBoundingClientRect();
  const rB = tgtEl.getBoundingClientRect();
  const dyA = rB.top - rA.top;
  const dyB = rA.top - rB.top;

  srcEl.style.transition = 'none';
  tgtEl.style.transition = 'none';
  srcEl.style.transform = `translateY(${dyA}px)`;
  tgtEl.style.transform = `translateY(${dyB}px)`;

  // 交换 DOM
  const p = srcEl.parentNode;
  const na = srcEl.nextSibling;
  const nb = tgtEl.nextSibling;
  if (na === tgtEl) { p.insertBefore(tgtEl, srcEl); }
  else if (nb === srcEl) { p.insertBefore(srcEl, tgtEl); }
  else { p.insertBefore(srcEl, nb); p.insertBefore(tgtEl, na); }

  // 触发动画
  requestAnimationFrame(() => {
    srcEl.style.transition = 'transform 0.25s ease';
    tgtEl.style.transition = 'transform 0.25s ease';
    srcEl.style.transform = '';
    tgtEl.style.transform = '';
  });

  lastSwapped = targetId;
}
```

- [ ] **Step 4: 实现 DnD 事件处理器**

```js
function onDragStart(e) {
  refreshItems();
  dragId = this.dataset.id;
  dragEl = this;
  lastSwapped = null;
  active = true;
  this.style.opacity = '0';
  e.dataTransfer.effectAllowed = 'move';
  e.dataTransfer.setData('text/plain', dragId);
}

// dragover/drop 绑定到 listEl，通过 e.target.closest 获取目标元素
function onDragOver(e) {
  e.preventDefault();
  if (!active || !dragId) return;
  e.dataTransfer.dropEffect = 'move';
  const target = e.target?.closest?.('.todo-item');
  if (!target) return;
  const tid = target.dataset.id;
  if (!tid || tid === dragId || tid === lastSwapped) return;
  if (crossedMidline(e.clientY, target)) performSwap(tid);
}

function onDrop(e) {
  e.preventDefault();
  if (!active) return;
  const finalOrder = Array.from(
    listEl.querySelectorAll('.todo-item:not(.drag-floating)')
  ).map(el => el.dataset.id);
  cleanup();
  if (opt.onSortEnd) opt.onSortEnd(finalOrder);
}

function cleanup() {
  removeFloating();
  if (dragEl) dragEl.style.opacity = '';
  dragId = null; dragEl = null; lastSwapped = null; active = false;
}
```

- [ ] **Step 5: 绑定 + 导出接口**

```js
function bind() {
  listEl.addEventListener('dragover', onDragOver);
  listEl.addEventListener('drop', onDrop);
  document.addEventListener('dragend', cleanup);
}
bind();
listEl._sortableData = { onDragStart, onTouchStart: null, onTouchMove: null, onTouchEnd: null };
```

- [ ] **Step 6: 在浏览器测试桌面拖动**

http://localhost:8000 — 鼠标拖动任务项，检查交换是否触发、动画是否平滑。

- [ ] **Step 7: Commit**

```bash
git add modules/todo/features/sortable.js
git commit -m "v1.5.0"
```

---

### Task 3: 移动端 Touch 支持

**Files:** Modify `modules/todo/features/sortable.js`

- [ ] **Step 1: 添加 Touch 状态**

```js
let touchTimer = null;
let touchDragging = false;
```

- [ ] **Step 2: 实现 Touch 处理器**

```js
function onTouchStart(e) {
  const el = this;
  const id = this.dataset.id;
  const cx = e.touches[0].clientX;
  const cy = e.touches[0].clientY;
  refreshItems();
  touchTimer = setTimeout(() => {
    touchTimer = null;
    touchDragging = true;
    dragId = id;
    dragEl = el;
    lastSwapped = null;
    active = true;
    el.style.opacity = '0';
    floating = createFloating(el, el.getBoundingClientRect().left, cy - 30);
  }, opt.touchDelay);
}

function onTouchMove(e) {
  if (touchDragging) {
    e.preventDefault();
    if (floating) floating.style.top = (e.touches[0].clientY - 30) + 'px';
    const target = document.elementFromPoint(
      floating?.getBoundingClientRect().left + (floating?.offsetWidth || 0) / 2 || e.touches[0].clientX,
      e.touches[0].clientY
    )?.closest('.todo-item');
    if (target?.dataset?.id && target.dataset.id !== dragId && target.dataset.id !== lastSwapped) {
      if (crossedMidline(e.touches[0].clientY, target)) performSwap(target.dataset.id);
    }
    return;
  }
  if (touchTimer) { clearTimeout(touchTimer); touchTimer = null; }
}

function onTouchEnd() {
  if (touchTimer) { clearTimeout(touchTimer); touchTimer = null; }
  if (touchDragging) {
    const finalOrder = Array.from(
      listEl.querySelectorAll('.todo-item:not(.drag-floating)')
    ).map(el => el.dataset.id);
    cleanup();
    touchDragging = false;
    if (opt.onSortEnd) opt.onSortEnd(finalOrder);
  }
}
```

- [ ] **Step 3: 更新 sortableData 导出**

```js
listEl._sortableData = { onDragStart, onTouchStart, onTouchMove, onTouchEnd };
```

- [ ] **Step 4: 在移动端浏览器测试**

http://localhost:8000 → 手机或 DevTools 移动模拟，长按 300ms 后拖动。

- [ ] **Step 5: Commit**

```bash
git add modules/todo/features/sortable.js
git commit -m "v1.5.0"
```

---

### Task 4: 集成到 app.js

**Files:** Modify `modules/todo/app.js`

- [ ] **Step 1: 添加 initSortable()**

在 `/* ── 初始化 ── */` 前插入：

```js
/* ── 拖拽排序 ────────────────────────────────────────── */

let sortableData = null;

function initSortable() {
  if (!todoList) return;
  enableSortable(todoList, {
    onSortEnd(newOrder) {
      const map = new Map(todos.map(t => [String(t.id), t]));
      todos = [];
      for (const id of newOrder) {
        const t = map.get(id);
        if (t) { todos.push(t); map.delete(id); }
      }
      save();
      render();
    },
    isSameGroup(id1, id2) {
      const t1 = todos[findIndex(+id1)];
      const t2 = todos[findIndex(+id2)];
      if (!t1 || !t2) return false;
      return t1.completed === t2.completed;
    },
    touchDelay: 300,
  });
  sortableData = todoList._sortableData;
}
```

- [ ] **Step 2: 在 render() 的 li 创建后绑定事件**

在 `const li = document.createElement('li');` 下面，`addLongPress(li, todo.id);` 前面：

```js
if (sortableData) bindSortableItem(li, sortableData);
```

- [ ] **Step 3: 修改初始化顺序**

末尾改为：

```js
initSortable();
render();
```

- [ ] **Step 4: 测试**

桌面拖动和移动端长按拖动均正常工作，排序后刷新数据持久。

- [ ] **Step 5: Commit**

```bash
git add modules/todo/app.js
git commit -m "v1.5.0"
```

---

### Task 5: CSS 样式

**Files:** Modify `modules/todo/style.css`

- [ ] **Step 1: 添加拖拽样式**

```css
.todo-item {
  transition: transform 0.25s ease;
}

.drag-floating {
  transition: none !important;
}
```

- [ ] **Step 2: 验证浮动克隆样式正确显示**

拖拽时检查：浮动元素 `z-index: 500` 位于所有元素上方，有阴影，`scale(1.02)` 微微放大。

- [ ] **Step 3: Commit**

```bash
git add modules/todo/style.css
git commit -m "v1.5.0"
```

---

### Task 6: 最终验证与清理

- [ ] **Step 1: 桌面端完整路径**
  1. 打开 http://localhost:8000
  2. 添加 3+ 条任务
  3. 鼠标拖动任务，验证交换动画
  4. 刷新页面，验证顺序保持
  5. 确认活跃/已完成互不跨区

- [ ] **Step 2: 移动端完整路径**
  1. DevTools 切换到移动视图
  2. 长按 300ms 拖拽
  3. 验证浮动克隆跟随手指
  4. 验证交换和保持

- [ ] **Step 3: 边界情况**
  1. 只有 2 条任务时拖动
  2. 快速连续拖动多次
  3. 角落：拖到列表顶部/底部之外

- [ ] **Step 4: Commit**

```bash
git commit -m "v1.5.0"
```
